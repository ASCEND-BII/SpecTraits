---
title: "PLSR Model Report"
subtitle: "Partial Least Squares Regression Analysis"
format:
  pdf:
    toc: true
    toc-depth: 2
    number-sections: true
    colorlinks: true
    geometry:
      - top=20mm
      - left=20mm
      - right=20mm
      - bottom=20mm
    fontsize: 11pt
    fig-pos: 'H'
    keep-tex: false
  html:
    toc: true
    toc-depth: 2
    toc-location: left
    number-sections: true
    embed-resources: true
    smooth-scroll: true
    theme: cosmo
    code-fold: true
    fig-width: 9
    fig-height: 6
params:
  author: "SpecTraits User"
  date: "today"
  seed: 42
  trait_selector: "trait"
  split_method: NULL
  press_method: NULL
  press_frame: NULL
  final_method: NULL
  final_PLSR: NULL
  results_predict: NULL
  perf_train: NULL
  perf_test: NULL
  fig_spectra: NULL
  fig_trait_hist: NULL
  fig_split_spectra: NULL
  fig_split_trait: NULL
  fig_press: NULL
  fig_coefficients: NULL
  fig_vip: NULL
  fig_train_scatter: NULL
  fig_train_histogram: NULL
  fig_train_residuals: NULL
  fig_test_scatter: NULL
  fig_test_histogram: NULL
  fig_test_residuals: NULL
---

\newpage

# Report Information

**Trait of Interest:** `r params$trait_selector`\
**Author:** `r params$author`\
**Date:** `r params$date`\
**Analysis Method:** Partial Least Squares Regression (PLSR)

This report presents the results of a Partial Least Squares Regression (PLSR) model 
developed to predict **`r params$trait_selector`** from leaf spectral reflectance data.

```{r}
#| echo: false
#| include: false

library(data.table)
library(knitr)
opts_chunk$set(out.width = "100%", fig.align = "center")
results_dt <- as.data.table(params$results_predict)
```

**Key Findings:**

- Total samples: **`r nrow(params$results_predict)`**
- Training samples: **`r nrow(results_dt[Dataset == "Training"])`**
- Testing samples: **`r nrow(results_dt[Dataset == "Testing"])`**
- Optimal components: **`r params$final_method$ncomp`**

# Model Configuration

- **Random Seed**: `r params$seed`
- **Split Method**: `r params$split_method$split`
- **Number of Components**: `r params$final_method$ncomp`
- **PLSR Method**: `r params$final_method$method`
- **Iterations**: `r params$final_method$iterations`

# Data Exploration

## Trait and Spectra Visualization

```{r fig-import-spectra-trait, echo=FALSE, warning=FALSE, fig.show='hold', out.width=c('48%','48%'), fig.cap="Spectral signatures (left) and trait distribution (right)"}
knitr::include_graphics(params$fig_spectra)
knitr::include_graphics(params$fig_trait_hist)
```

\newpage

# Data Split Visualization

```{r fig-data-split-spectra-trait, echo=FALSE, warning=FALSE, fig.show='hold', out.width=c('48%','48%'), fig.cap="Spectral data split (left) and trait distribution (right) showing training and testing samples"}
knitr::include_graphics(params$fig_split_spectra)
knitr::include_graphics(params$fig_split_trait)
```

\newpage

# PLSR Model Development
## Component Selection

The optimal number of components was determined using cross-validation.

Optimal number of components: `r params$final_method$ncomp`

```{r}
#| label: fig-press-statistics
#| fig-cap: "PRESS statistics for optimal components"
#| echo: false
#| warning: false

knitr::include_graphics(params$fig_press)
```

\newpage

## Model Coefficients and VIP

The PLSR regression coefficients and Variable Importance in Projection (VIP) scores.

```{r fig-coefficients-vip, echo=FALSE, warning=FALSE, fig.show='hold', out.width=c('48%','48%'), fig.cap="PLSR regression coefficients (left) and Variable Importance in Projection (VIP) scores (right)"}
knitr::include_graphics(params$fig_coefficients)
knitr::include_graphics(params$fig_vip)
```

\newpage

## Model Performance Assessment

### Training Dataset Performance

```{r fig-training-performance, echo=FALSE, warning=FALSE, fig.show='hold', out.width=c('32%','32%','32%'), fig.cap="Observed vs. predicted values (left), distribution comparison (center), and residuals (right) for training dataset"}
knitr::include_graphics(params$fig_train_scatter)
knitr::include_graphics(params$fig_train_histogram)
knitr::include_graphics(params$fig_train_residuals)
```

### Testing Dataset Performance

```{r fig-testing-performance, echo=FALSE, warning=FALSE, fig.show='hold', out.width=c('32%','32%','32%'), fig.cap="Observed vs. predicted values (left), distribution comparison (center), and residuals (right) for testing dataset"}
knitr::include_graphics(params$fig_test_scatter)
knitr::include_graphics(params$fig_test_histogram)
knitr::include_graphics(params$fig_test_residuals)
```

### Performance Metrics Summary

```{r}
#| label: tbl-performance-metrics
#| tbl-cap: "Model performance metrics"
#| echo: false
#| warning: false

library(knitr)

train_perf <- as.data.frame(params$perf_train)
test_perf  <- as.data.frame(params$perf_test)

if (params$final_method$method == "permutation") {
  perf_table <- data.frame(
    Metric       = train_perf$Parameter,
    Training_Mean = train_perf$Mean,
    Training_SD  = train_perf$SD,
    Testing_Mean = test_perf$Mean,
    Testing_SD   = test_perf$SD
  )
} else {
  perf_table <- data.frame(
    Metric      = train_perf$Parameter,
    Training    = train_perf$final,
    Training_SD = train_perf$SD,
    Testing     = test_perf$final,
    Testing_SD  = test_perf$SD
  )
}

kable(perf_table, align = c('l', 'c', 'c', 'c', 'c'), booktabs = TRUE)
```

\newpage

# Software Information

**SpecTraits: Shiny application for spectral trait prediction**

If you use this model or SpecTraits in your research, please cite:

Citation

Guzmán, J.A. and Cavender-Bares, J. (2026). SpecTraits: A Shiny Application for Prediction of Leaf Traits from Spectral Models. Version 0.2, GitHub: https://github.com/ASCEND-BII/SpecTraits

Contact: "aguzman@fas.harvard.edu"

Report Generated: `r format(Sys.time(), "%Y-%m-%d %H:%M:%S")`
